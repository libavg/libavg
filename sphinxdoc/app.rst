App Package
===========

The libavg.app package contains modules that manage the initialization and configuration
of a libavg application.

.. note::

    The app package is experimental. Functionality and interface are still in flux and
    subject to change.

.. automodule:: libavg.app
    :no-members:

