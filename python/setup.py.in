import glob
from distutils.core import setup

setup(name='libavg',
    version='${AVG_VERSION_RELEASE}',
    packages=['libavg', 'libavg.app', 'libavg.widget'],
    package_data={'libavg': ['data/*', 'shaders/*', 'avg.so']},
    scripts=glob.glob('scripts/avg_*'),
)
