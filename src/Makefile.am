if ENABLE_PARPORT
    PARPORT_SUBDIR = parport
else
    PARPORT_SUBDIR =
endif
SUBDIRS = base graphics player conradrelais $(PARPORT_SUBDIR) python

EXTRA_DIST = test/*.avg test/*.py test/*.png test/*.jpg test/*.tif \
    test/test.m1v test/test.m2v avgrc
